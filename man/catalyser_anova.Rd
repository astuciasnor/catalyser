% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analises.R
\name{catalyser_anova}
\alias{catalyser_anova}
\title{ANOVA de um fator}
\usage{
catalyser_anova(dados, p)
}
\arguments{
\item{dados}{Um data.frame com as observacoes individuais (uma linha por
individuo, nao medias ja calculadas).}

\item{p}{Lista de parametros com, no minimo, \code{resposta} (a variavel numerica)
e \code{fator} (a variavel de grupo). Aceita ainda \code{nivel_confianca} (padrao
0,95), \code{titulo_grafico}, \code{rotulo_x}, \code{rotulo_y} e \code{tema}.}
}
\value{
Lista com \code{narrativa}, \code{descritivos}, \code{tabela}, \code{comparacoes},
\code{grafico}, \code{pressupostos}, \code{diagnosticos}, \code{console} e \code{objeto}.
}
\description{
Compara a media de uma variavel numerica entre tres ou mais grupos e devolve,
de uma vez, tudo o que um relatorio precisa: resumo por grupo com media e
desvio-padrao, tabela da ANOVA, tamanhos de efeito, comparacoes de Tukey com
letras de diferenca, testes de pressupostos, grafico de barras e a narrativa
em portugues.
}
\details{
Linhas com dado faltante na resposta ou no fator sao removidas e \strong{contadas}:
o numero aparece em \code{diagnosticos}. Nada some em silencio.
}
\examples{
\dontrun{
resultado <- catalyser_anova(
  dados_analise,
  list(resposta = "profundidade_m", fator = "especie")
)
resultado$grafico       # barras com IC e letras
resultado$descritivos   # media +- DP e letras
cat(resultado$narrativa)
}
}
\seealso{
\code{\link[=catalyser_letras_tukey]{catalyser_letras_tukey()}} para as letras de diferenca.
}
